App\Entity\ResetPasswordRequest:
    reset_request_fixture:
        __construct:
            - '@user_14'
            - '<(new \DateTimeImmutable("+1 hour"))>'
            - 'fixture_selector'
            - '<hashPassword("fixture_verifier")>'
    reset_request_expired:
        __construct:
            - '@user_15'
            - '<(new \DateTimeImmutable("-1 hour"))>'
            - 'expired_selector'
            - '<hashPassword("expired_verifier")>'
